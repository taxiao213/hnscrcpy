name: build

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:

permissions:
  contents: write # release job 需要创建 GitHub Release

jobs:
  test:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: maven
      - run: mvn -B test

  package:
    needs: test
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-14
            label: mac-arm64
          - os: macos-13
            label: mac-x64
          - os: windows-latest
            label: windows-x64
          - os: ubuntu-latest
            label: linux-x64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: maven
      - name: Install deb packaging tools (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y fakeroot binutils
      - name: Package (macOS/Linux)
        if: runner.os != 'Windows'
        run: ./scripts/build-package.sh
        env:
          INSTALLER: "1" # CI 产出安装包；本地默认只出免安装 app-image
      - name: Package (Windows)
        if: runner.os == 'Windows'
        run: ./scripts/build-package.ps1
        env:
          INSTALLER: "1"
      # 统一整理成规范命名的发布产物：hnscrcpy-<平台>.<格式>
      - name: Stage release asset (macOS)
        if: runner.os == 'macOS'
        run: |
          mkdir -p target/release-assets
          cp target/dist/*.dmg "target/release-assets/hnscrcpy-${{ matrix.label }}.dmg"
      - name: Stage release asset (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force target/release-assets | Out-Null
          Copy-Item target/dist/*.exe "target/release-assets/hnscrcpy-${{ matrix.label }}-setup.exe"
      - name: Stage release assets (Linux)
        if: runner.os == 'Linux'
        run: |
          mkdir -p target/release-assets
          cp target/dist/*.deb "target/release-assets/hnscrcpy-${{ matrix.label }}.deb"
          tar -C target/dist -czf "target/release-assets/hnscrcpy-${{ matrix.label }}.tar.gz" hnscrcpy
      - uses: actions/upload-artifact@v4
        with:
          name: hnscrcpy-${{ matrix.label }}
          path: target/release-assets/*
          if-no-files-found: error

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: package
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: hnscrcpy-*
          path: dist
          merge-multiple: true
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
